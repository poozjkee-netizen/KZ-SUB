# Раздача плагина пользователям (.zxp)

Чтобы человек поставил плагин **без режима разработчика**, панель нужно
упаковать в подписанный `.zxp`. Ниже — как ТЫ собираешь `.zxp` и как КЛИЕНТ его
ставит.

---

## Часть 1. Сборка `.zxp` (делаешь ты, один раз на версию)

### 1. Получить инструмент ZXPSignCmd
Это официальная утилита Adobe для подписи расширений. Варианты, как достать:

- **Проще всего:** скачай готовый бинарник `ZXPSignCmd` для macOS из репозитория
  Adobe CEP-Resources или зеркал (поиск: «ZXPSignCmd macOS download»). Положи,
  например, в `~/bin/ZXPSignCmd`.
- Сними «карантин» macOS, иначе система не даст запустить:
  ```bash
  xattr -d com.apple.quarantine ~/bin/ZXPSignCmd 2>/dev/null || true
  chmod +x ~/bin/ZXPSignCmd
  ```

### 2. Собрать подписанный `.zxp`
```bash
cd ~/kz-sub
ZXPSIGNCMD=~/bin/ZXPSignCmd bash scripts/build-zxp.sh
```
Скрипт:
1. создаёт самоподписанный сертификат `dist/kzsub-cert.p12` (один раз);
2. подписывает папку `plugin/` в `dist/NP-SUB-<версия>.zxp` (версия из манифеста);
3. собирает **установщик одним кликом** `dist/NP-SUB-<версия>-installer.zip`.

Результат — два файла в `dist/`:
- **`NP-SUB-<версия>.zxp`** — для тех, кто ставит через ZXP/UXP Installer.
- **`NP-SUB-<версия>-installer.zip`** — установщик одним кликом (Windows `.bat`
  + macOS `.command`): сам кладёт панель и включает `PlayerDebugMode`, без ZXP
  Installer и без ручной правки реестра. **Рекомендуется большинству клиентов** —
  обходит частую ошибку ZXP Installer `-193` на Windows. Исходники установщика —
  `scripts/dist-installer/`.

> Сертификат самоподписанный — этого достаточно для установки через ZXP-инсталлер.
> Для публикации на **Adobe Exchange** позже понадобится их процедура ревью
> (самоподписи хватает для прямой раздачи клиентам).

### 3. Когда нужен новый `.zxp` (важно!)

| Что изменил | Нужен новый `.zxp`? |
|---|---|
| Панель: `index.html`, `css`, `js`, `jsx`, пресет, иконки | **Да** — пересобрать и раздать |
| Бэкенд: качество/нарезка/пунктуация, квоты, ключи, device-binding на сервере, новые языки на сервере | **Нет** — только `fly deploy`, пользователи получают сразу |

Правило: `.zxp` = «морда» плагина на компьютере пользователя. Всё, что считается
на сервере, обновляется деплоем шлюза и не требует переустановки у клиентов.

### 4. Перевыпуск при обновлениях панели
1. Подними версию в `plugin/CSXS/manifest.xml` (`ExtensionBundleVersion` **и**
   `Extension Version`) — иначе установщик не распознает обновление.
2. Снова запусти `build-zxp.sh` (тем же сертификатом — не удаляй
   `dist/kzsub-cert.p12`, иначе для Premiere это будет «другой» плагин).
3. Раздай новый файл пользователям (ставится поверх старого).

---

## Часть 2. Инструкция для клиента (даёшь вместе с файлом)

**Что нужно:** архив `NP-SUB-<версия>-installer.zip` и ключ активации (выдаёшь ты).
Установщик кроссплатформенный — один архив и для **macOS**, и для **Windows**.

### Способ A (рекомендуется) — установщик одним кликом
1. Распаковать `NP-SUB-<версия>-installer.zip` полностью.
2. В распакованной папке:
   - **Windows:** двойной клик по `install-windows.bat` (админ-права не нужны;
     если сработает SmartScreen — «Подробнее» → «Выполнить в любом случае»).
   - **macOS:** двойной клик по `install-macos.command` (если macOS ругается на
     «неустановленного разработчика» — правый клик по файлу → «Открыть» → «Открыть»).
3. Дождаться «Готово!».
4. Полностью перезапустить Premiere Pro → **Window → Extensions → NP SUB**.
5. Открыть ⚙, ввести **ключ активации** — панель готова.

> Установщик сам кладёт панель в папку расширений и включает `PlayerDebugMode`
> для всех версий Premiere (CEP 9–13). Внутри архива есть `README.txt` с теми
> же шагами — можно давать клиенту как есть.

### Способ B (альтернатива) — через ZXP/UXP Installer
1. Скачать **ZXP/UXP Installer** (aescripts) или **Anastasiy's Extension Manager**.
2. Перетащить файл `NP-SUB-<версия>.zxp` → **Install**.
3. Перезапустить Premiere → **Window → Extensions → NP SUB** → ввести ключ.

> **Ошибка ZXP Installer `-193` («Extension Manager init failed / a required
> resource is missing»)** на Windows — известный баг установщика (не плагина).
> Лечится: запустить/перезапустить приложение Adobe Creative Cloud и повторить;
> либо просто использовать **Способ A** — он этого установщика не требует.

### Использование
- Открыть секвенцию с казахской речью → кнопка **«СУБТИТР ЖАСАУ»**.
- Через несколько секунд субтитры появятся на новой дорожке субтитров.

---

## Продажи через Kaspi + выдача ключей

Два пути выдачи — выбирай любой, ключи попадают в ту же БД лицензий:

- **Telegram-бот** (`app/telegram_bot.py`) — клиент сам получает Demo сразу
  (`/demo`) и запрашивает Standard (`/buy`); ты только подтверждаешь оплату
  кнопкой в боте, ключ уходит клиенту автоматически. Настройка — в
  [`backend/README.md`](../backend/README.md#telegram-бот-выдачи-ключей-apptelegram_botpy).
  Рекомендуемый способ — меньше твоей ручной работы.
- **CLI напрямую** (ниже) — когда бота нет под рукой или для разовых случаев.

### Ручной цикл через CLI

Цикл продажи (без автоматизации — фолбэк, если бот недоступен):

1. **Клиент оплачивает на Kaspi** (перевод/QR) — единственный платный тариф
   **Standard**: $3 / 1 490 ₸, 60 минут или 30 дней (что раньше). Каталог:
   `python -m app.licenses plans`.
2. **Ты выдаёшь ключ** (в консоли шлюза):
   ```bash
   fly ssh console
   cd /app
   python -m app.licenses create --email client@mail --plan standard
   ```
   Скопируй выданный `api_key` (`kzsub_…`).
3. **Отправь клиенту:** ключ + файл `NP-SUB-<версия>.zxp` + инструкцию из «Часть 2».

### Управление лицензиями
```bash
python -m app.licenses list                       # все ключи + остаток минут/срок
python -m app.licenses topup <ключ> --minutes 100 # добить минут (доплата)
python -m app.licenses renew <ключ> --days 30     # продлить подписку (сброс минут)
python -m app.licenses revoke <ключ>              # отозвать (возврат/абьюз)
python -m app.licenses delete <ключ>              # удалить навсегда
```

Каждый ключ работает максимум на `KZSUB_MAX_DEVICES_PER_KEY` устройствах
(по умолчанию 2) — защита от передачи ключа посторонним.

> Тарифы задаются в `backend/app/plans.py` (единый источник, синхрон с
> `docs/MONETIZATION.md`) — меняешь цену/минуты там.
> Легаси `KZSUB_API_KEYS` ещё работает как бутстрап (заносится в БД при старте),
> но основной путь выдачи — CLI выше.

### Автоматизация (когда поток вырастет)
Шов готов: `licenses.create_from_plan(email, plan)`. Останется подключить
провайдера (вебхук оплаты / Telegram-бот) — он вызовет эту функцию после платежа.
См. «Задел под будущее» в `docs/MONETIZATION.md`.
