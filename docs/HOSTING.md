# Хостинг NP SUB (чтобы плагином могли пользоваться все)

«Дать доступ всем» = **две независимые задачи**:

1. **Поднять бэкенд** на публичном HTTPS-адресе (эта страница).
2. **Раздать плагин** — подписать `.zxp` и выложить на Adobe Exchange
   (см. раздел «Дистрибуция плагина» ниже).

---

## Главное ограничение: `large-v3` хочет GPU

На CPU `large-v3` работает медленно (медленнее реального времени) — для одного
пользователя терпимо, для публичного сервиса нет. Поэтому для хостинга нужен
**GPU** (или managed-ASR, но ты выбрал Whisper за качество).

## Варианты хостинга

| Вариант | Плюсы | Минусы | Когда |
|---|---|---|---|
| **Serverless GPU** (Runpod Serverless, Modal, Replicate) | платишь только за секунды обработки, **масштаб до нуля** — нет платы за простой | чуть больше настройки, «холодный старт» на первый запрос | **старт / низкий и рваный трафик** ← рекомендую |
| **GPU-VPS/инстанс** (аренда карты T4/A10) | самая низкая цена за минуту при высокой загрузке, полный контроль | платишь за простой 24/7, сам админишь | когда поток стабильный и большой |
| **Managed ASR API** (Yandex SpeechKit kk-KZ и т.п.) | без GPU и без ops | зависимость от вендора, платишь за минуту, не твой Whisper | как запасной движок |

**Рекомендация на запуск:** serverless-GPU (Runpod/Modal). Пока пользователей
мало — платишь копейки (только за реальную транскрибацию), при росте — переезд
на выделенный GPU, когда утилизация оправдает постоянную аренду.

## Самый дешёвый старт: Runpod Serverless (рекомендация)

Оплата **посекундно и только за время транскрибации**; при нуле запросов — $0.
Ориентир цены: RTX 4090/A5000-класс ≈ $0.0003–0.0006/сек ⇒ **минута обработки
≈ $0.02–0.04**, а минута обработки покрывает несколько минут аудио (large-v3 на
GPU быстрее реального времени). Прочувствуй: 100 роликов по 10 минут в месяц ≈
единицы долларов.

Готовые файлы: `backend/serverless/runpod_handler.py` + `Dockerfile.runpod`.

Шаги:
1. Аккаунт на runpod.io + Docker Hub.
2. Собрать и запушить образ (из папки `backend/`):
   ```bash
   docker build --platform linux/amd64 -f serverless/Dockerfile.runpod -t <you>/kzsub-runpod:v1 .
   docker push <you>/kzsub-runpod:v1
   ```
3. Runpod → Serverless → New Endpoint → образ `<you>/kzsub-runpod:v1`,
   GPU 24 GB (4090/A5000), Max Workers 1–2, **Idle Timeout 60–200 c**,
   **Container Disk ≥ 30 GB**, **FlashBoot ON**, разрешить **несколько
   дата-центров** (см. раздел «Надёжность» ниже), env `KZSUB_API_KEYS=секрет:pro`.
4. Тест: POST на `https://api.runpod.ai/v2/<endpoint-id>/runsync` с JSON
   `{"input": {"api_key": "...", "audio_base64": "..."}}`.

## Надёжность холодного старта (без Network Volume)

Веса модели **запечены в образ** (см. `Dockerfile.runpod`, ADR-7/ADR-11). Это
осознанный выбор: так эндпоинт не привязан к одному дата-центру и берёт GPU там,
где есть свободная карта. Для стабильных и быстрых стартов настрой эндпоинт так:

| Настройка | Значение | Зачем |
|---|---|---|
| **Container Disk** | **≥ 30 GB** | Образ ~8 ГБ, в распакованном виде ~18 ГБ. Мало места → пулл виснет на `image pull: pending` навсегда. Это причина №1 «вечного спиннера». |
| **FlashBoot** | **ON** | Runpod кэширует образ у себя — повторные холодные старты за секунды, без повторного пулла. |
| **Idle Timeout** | **60–200 c** | Воркер остаётся тёплым между роликами в рамках сессии монтажа → медленный только самый первый запрос. |
| **Data Centers** | **несколько** | Не запирайся в одном регионе. В дефицитных ДЦ (напр. Исландия `Low Supply`) один регион = очередь за GPU. |
| **Max Workers** | **1–2** | Больше на старте не нужно (иначе плодишь параллельные холодные пуллы). |

**Диагностика «висит на pending»:** почти всегда мало **Container Disk** — подними
до 30 GB и пересоздай воркер (Workers → Terminate). Первый пулл образа на новом
хосте — минуты (разово), дальше FlashBoot делает старты быстрыми.

> Network Volume (лёгкий образ + веса на сетевом диске) НЕ используем: том
> привязан к одному дата-центру и в дефицитных регионах душит доступность GPU.
> Вернуться к нему стоит только при высоком трафике, сконцентрированном в ДЦ с
> хорошим запасом карт (см. ADR-11).

**Прокси-режим (реализован):** панель говорит со ШЛЮЗОМ — нашим FastAPI на
копеечном CPU-хостинге; шлюз проверяет ключи/квоты и гоняет аудио в Runpod.
Runpod-ключ живёт только на шлюзе.

### Полный деплой за 6 шагов

1. **Собери и запушь GPU-образ воркера** (нужен Docker Hub):
   ```bash
   cd backend
   docker build -f serverless/Dockerfile.runpod -t <you>/kzsub-runpod:v1 .
   docker push <you>/kzsub-runpod:v1
   ```
2. **Runpod → Serverless → New Endpoint**: образ `<you>/kzsub-runpod:v1`,
   GPU 24 GB (4090/A5000), Max Workers 1–2, Idle Timeout 5–10 с, env:
   `KZSUB_API_KEYS=gateway-internal:pro`.
   Запиши **Endpoint ID** и создай **Runpod API Key** (Settings → API Keys).
3. **Собери образ шлюза**:
   ```bash
   docker build -f Dockerfile.gateway -t <you>/kzsub-gateway:v1 .
   docker push <you>/kzsub-gateway:v1
   ```
4. **Разверни шлюз** на любом CPU-хостинге с HTTPS (Fly.io / Railway /
   Render / $5 VPS + Caddy) с переменными:
   ```
   KZSUB_API_KEYS=<секретный-ключ-1>:pro,<триальный>:free
   KZSUB_RUNPOD_ENDPOINT_ID=<endpoint id из шага 2>
   KZSUB_RUNPOD_API_KEY=<runpod api key>
   KZSUB_RUNPOD_WORKER_KEY=gateway-internal
   ```
5. **Проверь**: `curl https://<шлюз>/health`, затем тестовая транскрибация
   с ключом из KZSUB_API_KEYS.
6. **Направь панель**: в `plugin/index.html` поставь URL шлюза значением по
   умолчанию поля `#apiUrl` (и ключ пользователя — в `#apiKey` через
   localStorage при выдаче), собери ZXP и раздавай.

> Первый запрос после простоя будет с «холодным стартом» GPU (десятки секунд —
> качаются веса, если не используешь Network Volume). Совет: включи в Runpod
> **FlashBoot** и/или прикрепи **Network Volume** с кэшем HuggingFace —
> старт станет заметно быстрее.

## Анти-шаринг: привязка ключа к устройствам

Панель шлёт `X-Device-Id` (хэш от характеристик машины). Шлюз привязывает ключ
к первым **N устройствам** (`KZSUB_MAX_DEVICES_PER_KEY`, по умолчанию 2 —
основной комп + ноутбук). Третье устройство получает 403.

Привязки хранятся в `KZSUB_STATE_DIR/devices.json` (на Fly — том `/data`,
переживает рестарты). Требуется один раз создать том:
```bash
fly volumes create kzsub_data --region fra --size 1 -y
```

**Поддержка «я сменил компьютер»:** сбросить привязку ключа —
```bash
fly ssh console -C "python3 -c \"import json;p='/data/devices.json';d=json.load(open(p));d.pop('КЛЮЧ_КЛИЕНТА',None);json.dump(d,open(p,'w'))\""
```
(или вручную: `fly ssh console` → отредактировать `/data/devices.json`).

Честно о границах: это защита от бытового шаринга («скинь ключ другу»), а не
абсолютная. Главный ограничитель расходов — серверные квоты по ключу.

## Деплой контейнера (общая схема, подходит любому провайдеру)

Бэкенд уже контейнеризован: `backend/Dockerfile` (CPU) и `backend/Dockerfile.gpu`.

```bash
# GPU-образ
docker build -f backend/Dockerfile.gpu -t kzsub-api:gpu backend

# Локальный прогон на GPU-хосте
docker run --gpus all -p 8000:8000 \
  -e KZSUB_API_KEYS="секрет-1:pro,триал:free" \
  -v hfcache:/root/.cache/huggingface \
  kzsub-api:gpu
```

Дальше — залить образ в реестр провайдера и запустить его сервисом. У Runpod/
Modal есть деплой прямо из образа или из репозитория.

### Что ОБЯЗАТЕЛЬНО настроить перед публичным доступом
- **`KZSUB_API_KEYS`** — свои секретные ключи. Иначе работает общеизвестный
  `dev-key`, и любой получит бесплатный безлимит.
- **HTTPS** — панель шлёт аудио и ключ по сети. Поставь сервис за TLS
  (у serverless-провайдеров HTTPS из коробки; на своём VPS — Caddy/nginx).
- **Том для весов** (`/root/.cache/huggingface`) — чтобы `large-v3` (~3 ГБ) не
  качался при каждом старте.

### CORS не нужен
Панель ходит на сервер через Node-`http` (не браузерный `fetch`), поэтому
CORS-настройка не требуется.

## Что доделать до реальных продаж (сейчас заглушки)
Это уже не про «поднять», а про «продавать» — но важно помнить:
- **Учёт квоты — в памяти процесса** (`quota.py`). При нескольких репликах или
  рестарте счётчик сбрасывается. Для биллинга нужна **общая БД** (Postgres/Redis).
- **Аутентификация — статичные ключи** из env. Для самообслуживания нужны
  регистрация и генерация ключей на сайте + оплата (Kaspi/Stripe).

Рекомендуемый порядок: сначала поднять бэкенд на serverless-GPU с секретными
ключами (раздаёшь ключи вручную первым пользователям/бете), потом — БД+оплата.

## Направить панель на прод-сервер
В панели поле **API URL** — вписать публичный `https://…`-адрес; значение
запоминается. Для массовой раздачи можно зашить прод-URL как значение по
умолчанию в `plugin/index.html` (поле `#apiUrl`).

## Дистрибуция плагина (вторая половина задачи)
Чтобы пользователи ставили панель без dev-режима CEP:
1. **Подписать `.zxp`** утилитой `ZXPSignCmd` (нужен сертификат подписи).
2. Выложить на **Adobe Exchange** (модерация Adobe) и/или дать `.zxp` +
   установщик (например, ZXP/UXP Installer) для прямой установки.

Подробные шаги подписи вынесем отдельно, когда бэкенд будет на проде.
