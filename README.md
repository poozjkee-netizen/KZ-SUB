# NP SUB — авто-субтитры на казахском для Adobe Premiere Pro

Плагин для Premiere Pro, который автоматически распознаёт **казахскую речь** и
вставляет субтитры в таймлайн. Premiere «из коробки» не транскрибирует казахский —
это и есть проблема, которую решает продукт.

## Как это устроено

```
┌─────────────────────────┐        ┌──────────────────────────────┐
│   Панель в Premiere      │        │       Бэкенд (NP SUB API)     │
│   (CEP, HTML/JS/JSX)     │        │       FastAPI + Whisper       │
│                          │        │                              │
│ 1. Выгружает аудио  ─────┼───────▶│ 2. Транскрибирует казахский   │
│    из активной секвенции │  POST  │    (faster-whisper, kk)       │
│                          │        │ 3. Сегменты → .srt            │
│ 5. Импортирует .srt  ◀───┼────────┤ 4. Возвращает .srt + JSON     │
│    как дорожку субтитров │        │    (+ учёт квоты/подписки)    │
└─────────────────────────┘        └──────────────────────────────┘
```

Ядро продукта — **не плагин, а движок распознавания казахского**. Плагин лишь
удобная обёртка внутри Premiere. Качество ASR = ценность, за которую платят.

## Структура репозитория

| Папка        | Что внутри                                                        |
|--------------|-------------------------------------------------------------------|
| `backend/`   | API транскрибации: приём аудио → казахский текст с тайм-кодами → SRT |
| `plugin/`    | CEP-панель для Premiere Pro (UI, выгрузка аудио, импорт субтитров) |
| `docs/`      | Архитектура, монетизация, roadmap                                 |

## Быстрый старт

**Первый запуск в Premiere — по шагам:** [`docs/RUNBOOK.md`](docs/RUNBOOK.md).

Кратко:
```bash
# 1. Установить панель в Premiere (macOS)
bash scripts/install-macos.sh          # Windows: scripts\install-windows.ps1

# 2. Запустить бэкенд
bash scripts/run-backend.sh            # Windows: scripts\run-backend.bat
```
Затем в Premiere: **Window → Extensions → NP SUB**, ключ `dev-key`, «Тест» →
выбрать `.epr` пресет → «Субтитр жасау».

Ручная установка и детали — [`plugin/README.md`](plugin/README.md),
[`backend/README.md`](backend/README.md).

## Статус

MVP-каркас. Что работает / что требует доработки — см. [`docs/ROADMAP.md`](docs/ROADMAP.md).

## Документация

- [Архитектура](docs/ARCHITECTURE.md)
- [Хостинг (деплой бэкенда для всех)](docs/HOSTING.md)
- [Монетизация и юнит-экономика](docs/MONETIZATION.md)
- [Roadmap](docs/ROADMAP.md)
