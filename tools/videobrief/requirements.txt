# Зависимости NP Brief (tools/videobrief). К бэкенду NP SUB отношения не имеют:
# программа запускается на маке и никуда не деплоится. Всё считается локально —
# ни ключей, ни облака.

# Скачивание метаданных, субтитров и аудио. Обновляй чаще остального: площадки
# меняют выдачу, и старая версия перестаёт открывать ссылки.
yt-dlp>=2024.8.6

# Распознавание речи, когда у ролика нет готовых субтитров.
faster-whisper>=1.1.0

# Разбор: читает файл модели .gguf прямо в процессе программы, без сервера.
# На маке собирается с поддержкой Metal (видеокарта). Если сборка из исходников
# долгая, ставь с готовыми колёсами:
#   pip install llama-cpp-python \
#     --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/metal
llama-cpp-python>=0.3.0
